name: Windows build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Restore
        run: dotnet restore IndigoRouterScheduler.csproj
      - name: Build
        run: dotnet build IndigoRouterScheduler.csproj -c Release --no-restore
      - name: Publish standalone Windows application
        run: dotnet publish IndigoRouterScheduler.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=None -p:DebugSymbols=false -o release/win-x64
      - uses: actions/upload-artifact@v4
        with:
          name: IndigoRouterScheduler-win-x64
          path: release/win-x64/IndigoRouterScheduler.exe
